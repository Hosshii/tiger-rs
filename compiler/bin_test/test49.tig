let 
    var count := 0
in 
    for i:= 0 to 10 do ( 
        count := count + i;
        for j:= 0 to i do (
            count := count + j;
            if j = 1 then break
        )
    );
    count
end
